version: "3.7"

services:
  # 1. Init container: Maakt de mappen aan EN schrijft direct de juiste ckpool.conf weg!
  init_permissions:
    image: alpine:3.22.1
    container_name: frackatack-init
    restart: "no"
    networks:
      - frackatack 
    volumes:
      - /DATA/AppData/frackatack/data:/data
    command:
      - /bin/sh
      - -ec
      - |-
        set -eu
        mkdir -p /data/ui/state /data/node /data/pool/config /data/pool/www/pool /data/pool/www/users
        
        # AUTOMATISCHE FIX: We genereren de missende ckpool.conf direct met de juiste inloggegevens
        if [ ! -f /data/pool/config/ckpool.conf ]; then
          echo '{"btcsolo":true,"btcaddress":"bc1qg09gugv3kk2sv8svq7zewmt6ey00gppkctgtzk","btcd":[{"url":"http://fractal:28342","user":"fractal","pass":"MijnSuperVeiligWachtwoord123"}],"ports":["3333"],"webdir":"/data/pool/www/pool","userdir":"/data/pool/www/users","loglevel":4}' > /data/pool/config/ckpool.conf
        fi

        chown -R 1000:1000 /data/ui /data/node /data/pool || true
        chmod -R ug+rwX /data/ui /data/node /data/pool || true

  # 2. De Frackatack Web UI App Dashboard
  app:
    image: ghcr.io/willitmod/fracattack-app-umbrel-dev:0.1.4
    container_name: frackatack-app
    user: "1000:1000"
    restart: unless-stopped
    networks:
      - frackatack
    ports:
      - "21225:3000/tcp" # UI bereikbaar op poort 21225
    depends_on:
      init_permissions:
        condition: service_completed_successfully
      fractal:
        condition: service_started
    volumes:
      - /DATA/AppData/frackatack/data:/data
      - /var/run/docker.sock:/var/run/docker.sock
    environment:
      APP_ID: "willitmod-dev-frackatack"
      APP_VERSION: "0.1.3"
      APP_CHANNEL: "RC1"
      APP_RELEASE_PHASE: "ALPHA"
      APP_SELF_RESTART_ENABLED: "1"
      APP_COMPOSE_PROJECT: "frackatack"
      NETWORK_IP: "127.0.0.1" 
      FB_NODE_IMAGE: "ghcr.io/willitmod/fracattack-fractald:0.3.1"
      CKPOOL_IMAGE: "ghcr.io/willitmod/docker-ckpool-solo:590fb2a"
      # HERSTELD: Toegevoegde FB_ prefixen voor de WebUI-communicatie
      FB_RPC_HOST: "fractal"
      FB_RPC_PORT: "28342"
      FB_RPC_USER: "fractal"
      FB_RPC_PASS: "MijnSuperVeiligWachtwoord123"
      CKPOOL_STRATUM_HOST: "ckpool"
      CKPOOL_STRATUM_PORT: "3333"
      JWT_SECRET: "sajkdh9a23hf8923hfshdf23"
    healthcheck:
      test:
        - CMD-SHELL
        - python -c "import sys, urllib.request; r=urllib.request.urlopen('http://127.0.0', timeout=3); sys.exit(0 if r.status == 200 else 1)"
      interval: 15s
      timeout: 5s
      retries: 20
      start_period: 90s

  # 3. De Fractal Bitcoin Core Node
  fractal:
    image: ghcr.io/willitmod/fracattack-fractald:0.3.1
    container_name: frackatack-node
    restart: unless-stopped
    stop_grace_period: 15m30s
    user: "0:0"
    networks:
      frackatack:
        aliases:
          - fractal
    depends_on:
      init_permissions:
        condition: service_completed_successfully
    environment:
      NETWORK: "mainnet"
      RPC_USER: "fractal"
      RPC_PASSWORD: "MijnSuperVeiligWachtwoord123"
      RPC_PORT: "28342"
      P2P_PORT: "28343"
      ZMQ_HASHBLOCK: "tcp://0.0.0.0:28345"
      EXTERNAL_IP: "127.0.0.1"
      FORCE_IPV4_BIND: "1"
      PRUNE: "0"
      RPC_THREADS: "4"
      MAX_MEMPOOL: "64"
      DB_CACHE: "256"
      TX_INDEX: "1"
    volumes:
      # HERSTELD: Gecorrigeerd naar het interne pad /root/.bitcoin zodat de node-map vult!
      - /DATA/AppData/frackatack/data/node:/root/.bitcoin
    ports:
      - "28343:28343/tcp"
    healthcheck:
      test:
        - CMD-SHELL
        - bitcoin-cli -rpcuser="fractal" -rpcpassword="MijnSuperVeiligWachtwoord123" getblockchaininfo >/dev/null 2>&1
      interval: 30s
      timeout: 10s
      retries: 20
      start_period: 2m

  # 4. De CKPool Stratum Engine
  ckpool:
    image: ghcr.io/willitmod/docker-ckpool-solo:590fb2a
    container_name: frackatack-ckpool
    restart: unless-stopped
    stop_grace_period: 30s
    networks:
      - frackatack
    depends_on:
      init_permissions:
        condition: service_completed_successfully
      fractal:
        condition: service_started
    environment:
      # HERSTELD: Toegevoegde FB_ prefixen conform de image specificaties
      FB_RPC_HOST: "fractal"
      FB_RPC_PORT: "28342"
      FB_RPC_USER: "fractal"
      FB_RPC_PASSWORD: "MijnSuperVeiligWachtwoord123"
      FB_ZMQ_HASHBLOCK_PORT: "28345"
      PAYOUT_ADDRESS: "bc1qg09gugv3kk2sv8svq7zewmt6ey00gppkctgtzk"
      MINDIFF: "1"
      STARTDIFF: "16"
      MAXDIFF: "0"
    user: "0:0"
    command: ["ckpool", "-k", "-L", "-c", "/config/ckpool.conf", "-B"]
    ports:
      - "7891:3333/tcp"
    volumes:
      - /DATA/AppData/frackatack/data/pool/config:/config
      - /DATA/AppData/frackatack/data/pool/www:/www
    healthcheck:
      test:
        - CMD-SHELL
        - '[ -f /config/ckpool.conf ] || exit 1'
      interval: 15s
      timeout: 5s
      retries: 20
      start_period: 45s

networks:
  frackatack:
    driver: bridge

x-casaos:
  author: WVK2017
  category: Crypto
  icon: https://githubusercontent.com
  index: /
  port_map: "21225"
  scheme: http
  title:
    custom: Frackatack Solo Pool
    
